machine:
    timezone:
        Asia/Singapore

    services:
        - docker
dependencies:
    pre:

deployment:
    production:
        branch: master
        commands:
            - su ./deploy.sh $CIRCLE_SHA1
