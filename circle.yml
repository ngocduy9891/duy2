machine:
    timezone:
        Asia/Singapore

    services:
        - docker
dependencies:
    pre:

deployment:
    production:
        branch: master
        commands:
            - ./deploy.sh $CIRCLE_SHA1
