machine:
    timezone:
        Asia/Singapore

    services:
        - docker
dependencies:
    pre:

deployment:
    production:
        branch: master
        commands:
            - sudo ./deploy.sh $CIRCLE_SHA1
